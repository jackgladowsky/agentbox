# git

Version control — commits, branches, remotes.

## CLI
- **Type:** prebuilt
- **Binary:** `git`
- **Install:** `sudo apt install git`

## Auth
- **Required:** no (SSH key for remote ops)

## Depends On
- none

## Commands
- `git add -A && git commit -m <message>`
- `git push`
- `git pull`
- `git status`
- `git log --oneline -20`
- `git checkout -b <branch>`
